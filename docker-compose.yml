services:
  db:
    image: mcr.microsoft.com/mssql/server:2025-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "${MSSQL_SA_PASSWORD}"
    volumes:
      - mssql-data:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$MSSQL_SA_PASSWORD\" -C -Q \"SELECT 1\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  api:
    build:
      context: .
      dockerfile: EncryptedChat.Api/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: "Production"
      ASPNETCORE_URLS: "http://+:8080"
      ConnectionStrings__Default: "Server=db;Database=EncryptedChat;User Id=sa;Password=${MSSQL_SA_PASSWORD};TrustServerCertificate=true"
      Jwt__Key: "${JWT_KEY}"
      Jwt__Issuer: "${JWT_ISSUER}"
      Jwt__Audience: "${JWT_AUDIENCE}"
      Cookies__Secure: "false"
      Cookies__SameSite: "Lax"
      DisableHttpsRedirection: "true"
      RunMigrationsOnStartup: "true"
      Sentry__Dsn: ""
      Giphy__ServiceApiKey: "${GIPHY_API_KEY}"
    volumes:
      - attachments:/app/storage/attachments
    depends_on:
      db:
        condition: service_healthy

  web:
    build:
      context: .
      dockerfile: EncryptedChat.Client/Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - api

volumes:
  mssql-data:
  attachments:
